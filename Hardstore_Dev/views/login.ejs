<!DOCTYPE html>
<html lang="en">
  <head>
      <%- include("./partials/head.ejs") %>
      <title>Login Hardstore</title>
      <link href="/CSS/login.css" rel="stylesheet" >
      <!-- iconos -->
      <link href='https://unpkg.com/boxicons@2.1.1/css/boxicons.min.css' rel='stylesheet'>
      <script src="https://code.iconify.design/2/2.1.2/iconify.min.js"></script>
  </head>
  <body>
      <header>
        <%- include("./partials/header.ejs") %>
      </header>
      <main>
        <header class="login">
          <h2>Iniciar Sesión</h2>
          <span class="iconify" data-icon="ion:person-outline"></span>
        </header>
        <form action="/login" method="POST">
          <input type="name" placeholder="Usuario" name="userName" 
          <% if (locals.old){ %>
            value=<%= old.userName %>
            <% } %>
          > 
          <input type="contraseña" value=""placeholder="Contraseña" name="password">
          <p><label><input type="checkbox" name="remember" value="true"> Recordarme </label></p>
          <a href="#">Recuperar contraseña</a>

          <div class="error">
            <% if (locals.error){ %>
              <p><%= error %></p>
            <% } %>
            <% if(locals.errors){ %>
              <% for (error of errors){ %>
                <p><%= error.msg %></p>
            <% }} %>
          </div>

          <button type="submit" class="mensaje">LOGIN</button>
        </form>
        <hr>                     
        <p>Ingresar con:</p>
        <div class= icons>
          <i class='bx bxl-instagram'></i>
          <span class="iconify-inline" data-icon="logos:google-gmail"></span>
          <i class='bx bxl-facebook-circle' ></i>
          </div>
        <p>¿Todavía no tenes cuenta?</p>
        <a href="/registrarme">Hace click acá</a>
      </main>
      <footer>
        <%- include("./partials/footer.ejs") %>
      </footer>
  </body> 
</html> 