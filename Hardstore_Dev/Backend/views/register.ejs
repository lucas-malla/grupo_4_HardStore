<!DOCTYPE html>
<html lang="en">
  <head>
      <%- include("./partials/head.ejs") %>
      <title>register Hardstore</title>
      <link href="/CSS/register.css" rel="stylesheet" >
      <!-- iconos -->
      <link href='https://unpkg.com/boxicons@2.1.1/css/boxicons.min.css' rel='stylesheet'>
      <script src="https://code.iconify.design/2/2.1.2/iconify.min.js"></script>
  </head>
  <body>
      <header>
        <%- include("./partials/header.ejs") %>
      </header>
      <main>
        <header class="register">
          <h2>Bienvenido a HARDSTORE</h2>
          <h5>Registrate y aprovecha descuentos exclusivos</h5>
        </header>
        <div class="errores">
          <ul>
            <% if(locals.errors){ %>
              <% for (error of errors){ %>
                <li><%= error.msg %></li>
            <% }} %>
          </ul>
        </div>
        <form id="formRegister" action="/register" method="POST" enctype="multipart/form-data">
          <input id="username" type="name" placeholder="Usuario" name="userName" value="<%= locals.old ? old.userName : '' %>" >
          <p class="nameError errores"></p>
          <input id="email" type="email" placeholder="email" name="email" value="<%= locals.old ? old.email : '' %>">
          <p class="emailError errores"></p>
          <input id="password" type="contraseña" placeholder="Contraseña" name="password">
          <p class="passwordError errores"></p>
          <input id="passwordRepeat" type="contraseña" placeholder="Repite tu contraseña" name="password_repeat">
          <p class="passwordRepeatnameError errores"></p>
          <p>Imagen de usuario</p>
          <input id="file" class="file" type="file" name="avatar">
          <button id="buttonSubmit" type="submit" class="mensaje">Registrarme</button>
        </form>
        <hr>                     
        <p>Registrate con:</p>
        <div class= icons>
          <i class='bx bxl-instagram'></i>
          <span class="iconify-inline" data-icon="logos:google-gmail"></span>
          <i class='bx bxl-facebook-circle' ></i>
          </div>
        <p>¿Ya tenes cuenta?</p>
        <a href="/login">Hace click acá</a>
      </main>
      <footer>
        <%- include("./partials/footer.ejs") %>
      </footer>
      <script src="js/register.js"></script>
  </body>
</html>