<!DOCTYPE html>
<html lang="en">

<head>
    <title>Hardstore</title>
    <link href="/CSS/prod_gal.css" rel="stylesheet">
    <link href="/CSS/footer.css" rel="stylesheet">
    <link href="/CSS/header.css" rel="stylesheet">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/190d2f0c87.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <header>
        <%- include("./partials/header.ejs") %>
    </header>
    <main>
        <div class="container-sm">
            <div class="filt-ord">
                <p class="filtro-desplegable">Filtrar: <i class="fas fa-sort-down"></i></p>
                <p class="ordenar-por">Ordenar por: <i class="fas fa-sort-down"></i></p>
            </div>
        <div style="max-width: 1000px; margin: 0 auto;">
        <div class="options crear"><a href="/admin/products/create" class="action-button edit crear">CREAR NUEVO</a>
        </div>
        <hr>
        <table class="table caption-top">
            <thead>
                <tr>
                    <th scope="col"></th>
                    <th scope="col">Título</th>
                    <th scope="col">Precio</th>
                    <th scope="col">Promoción</th>
                    <th scope="col">Descuento</th>
                    <th scope="col">Acción</th>
                </tr>
            </thead>
            <tbody>
                <% for (product of results){ %>
                    <a href="/products/<%= product.id %>">
                        <tr>
                            <th class="row"><div class="img"><img class="medida" src=/images/products/<%=product['images.image_name']%> ></div></th>
                            <td>
                                <%= product.product_name %>
                            </td>
                            <td >
                                <del> <%= product.price %></del>
                            </td>
                            <td><%= product.price * (100-product.discount)/100 %> </td>
                            <td>
                                <%= product.discount %>% OFF
                            </td>
                            <td>
                                <div class="options">
                                    <a href="/admin/products/<%= product.id %>/edit"
                                        class="action-button edit">MODIFICAR</a>
                                    <form action="/admin/delete/<%= product.id %>?_method=DELETE" method="POST">
                                        <button type="submit" class="action-button delete">ELIMINAR</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        <a>
                            <% } %>
            </tbody>
        </table>
        </div>
    </div>
    </main>

    <footer>
        <%- include("./partials/footer.ejs") %>
    </footer>
</body>

</html>